## 一、日志
2025/11
第 1 轮：从零创建
2026/3
第 2 轮：识别弱、模型糙、运行卡
- 识别能力弱 → 升级检测管线（多采样点检测、降采样推理提速）
- 模型不精致 → 重构程序化模特：LatheGeometry 曲面躯干、关节球、三点布光、接触阴影
- 运行卡顿 → 推理输入限宽（640px）、零分配热路径（预分配 Vector3/Quaternion 池）、UI 节流
2026/6~2026/9/3
第 3 轮：视频中完全识别不到人（关键攻坚）
1. 建立可复现的测试环境：本地资产镜像 + CORS 静态服务器 + Playwright 无头浏览器端到端自动化测试，把"识别不到"变成可重复、可观测的实验
2. 发现矛盾现象：同一套代码，引擎有时加载成功、有时报 `engine: null`——间歇性故障
3. 定位根因：通过控制台日志抓到 `Aborted(Module.arguments has been replaced...)`——两个 AI 引擎（tasks-vision 与旧版 Pose）的 WASM 都基于 Emscripten，并行初始化时在启动期争抢全局 `Module` 对象，互相破坏，导致崩溃
4. 验证假设：单独启用主引擎 → 100% 成功；并行加载 → 概率性双崩。假设证实
5. 修复方案：串行加载策略——先加载主引擎（BlazePose Heavy），失败或 40 秒超时后才启动旧版引擎兜底，就绪后自动升级接管
6. 连带问题：T-Pose 前臂反向（骨骼四元数缺少父系坐标补偿，改为 `qLocal = qParent⁻¹ × qWorld`）、CDN 资源 404（tasks-vision 只发 ESM 包，改动态 `import()`）、记录导入后回放时长显示 0.00s、折叠面板残字

最终验证：真实视频 + 真实模型推理的端到端测试全链路通过——检测 → 锁定 → 录制 → 自动保存 → 回放。

---

# 当前问题：
1. 快速动作模糊/跟丢
原因：视频帧率有限，快速运动产生运动模糊 + 帧间位移过大导致最近邻匹配失效。
换用支持时序的模型（如 MotionBERT、或 BlazePose 的视频模式平滑），利用前后帧上下文
2. 精细动作（手指、细微姿态）丢失
原因：BlazePose 33 关键点不含手指细节，且手部在 640px 输入下只有几个像素。
对部分区域做局部放大二次推理（先全身定位手腕，再裁切手腕等区域高清推理：如 EasyMocap、MediaPipe Holistic）
3. 转身/侧身
原因：单目 2D 关键点估计 3D 姿态，转身时深度信息丢失（z 是弱估计），自遮挡导致关键点不可见。
3.1：转身时利用 `visibility` 字段降权不可见关键点，用骨骼长度约束做 IK 补全（目前是直接使用原始值）
3.2：升级为真正的单目 3D 姿态估计模型（**MotionBERT、MHFormer、VideoPose3D**——输入 2D 关键点序列，输出时序一致的 3D 姿态，天然抗转身歧义）；或者多摄像头三角化

==>
当前: 单目 2D → 启发式 3D 映射
  ↓ 
+ 卡尔曼预测跟踪 + visibility 
  ↓ 
+ Holistic/HandLandmarker 局部放大 → 解决精细手部
  ↓ 
接入 VideoPose3D/MotionBERT 时序 3D 模型 


## 二、涉及的知识与技术
计算机视觉 / AI
- 人体姿态估计（BlazePose 33 关键点拓扑、归一化坐标系）
- 多目标检测与选择（numPoses 多人模式）
- 多目标追踪：最近邻数据关联（髋部中心距离）、丢失重捕获（阈值放宽 + 强制接受）、指数平滑滤波
- 多时间点采样检测策略（应对首帧遮挡/姿态不佳）
3D 图形学
- Three.js 场景图与层级骨骼（Group 树：Root→脊柱→四肢链）
- 四元数姿态求解：`setFromUnitVectors` 方向对齐 + **父系坐标补偿**（世界四元数转局部四元数）
- SLERP 球面插值平滑、时间轴线性插值重采样
- 程序化建模（LatheGeometry 旋转曲面）、三点布光
- 浏览器端视频导出管线：Canvas captureStream + MediaRecorder
浏览器工程
- WASM/Emscripten 运行时机制（全局 Module 冲突这个坑，本质是理解 Emscripten 模块加载原理才能诊断）
- Web 性能优化：零分配热路径、推理降采样、帧率自适应
- 状态机驱动的 UI 架构（单一 `updateButtons()` 入口保证 UI 闭环）
工程方法论
- 端到端自动化测试（Playwright 无头浏览器 + 合成数据注入，分层验证：真实推理验证检测、合成关键点验证管线）
- 假设驱动的调试：矛盾现象 → 日志取证 → 隔离变量 → 验证假设 → 修复 → 回归测试

---

## 三、关联

对通信工程的关联：

视频降采样后推理-------------------- 奈奎斯特采样与信号带宽/成本的权衡 
指数平滑滤波（0.7/0.3 权重）-------- 一阶 IIR 低通滤波器，抑制观测噪声 
最近邻髋部匹配 + 丢失重捕获---------- 目标跟踪中的数据关联（最近邻法），雷达/多目标跟踪同款思想 
追踪丢失 5 帧/3 秒分级告警告--------- 通信链路的分级超时与重传机制 
时间轴重采样对齐两段动作记录--------- 多路信号的时钟同步与重采样 
多视频加权融合---------------------- 分集接收合并（MRC 最大比合并的思想：按置信度加权） 
WASM 引擎串行初始化----------------- 共享资源互斥访问（临界区保护） 

对具身智能（Embodied AI）：

1. 动作捕捉是具身智能的数据入口
2. retargeting（重定向）问题
3. 观测噪声处理

---


